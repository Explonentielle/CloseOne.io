generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Test {
  id        Int      @id @default(autoincrement())
  message   String   @default("connexion ok !")
  createdAt DateTime @default(now())
}
